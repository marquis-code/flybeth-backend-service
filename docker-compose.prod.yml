services:
  reverse-proxy:
    image: traefik:v3.6
    restart: always
    command:
      - "--providers.docker=true"
      - "--providers.docker.exposedbydefault=false"
      - "--entryPoints.web.address=:80"
      - "--entryPoints.websecure.address=:443"
      - "--certificatesresolvers.flybethresolver.acme.tlschallenge=true"
      - "--certificatesresolvers.flybethresolver.acme.email=abahmarquis@gmail.com"
      - "--certificatesresolvers.flybethresolver.acme.storage=/letsencrypt/acme.json"
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - "./letsencrypt:/letsencrypt"
      - "/var/run/docker.sock:/var/run/docker.sock:ro"
    networks:
      - flybeth-net
    labels:
      - "traefik.enable=true"
      # HTTP-to-HTTPS Redirect
      - "traefik.http.routers.http-catchall.entrypoints=web"
      - "traefik.http.routers.http-catchall.rule=HostRegexp(`{host:.+}`)"
      - "traefik.http.routers.http-catchall.middlewares=redirect-to-https"
      - "traefik.http.middlewares.redirect-to-https.redirectscheme.scheme=https"

  redis:
    image: redis:7-alpine
    restart: always
    command: redis-server --save 20 1 --loglevel warning
    volumes:
      - redis_data:/var/lib/redis/data
    networks:
      - flybeth-net

  api:
    image: "ghcr.io/viyn-labs/cloza-backend:main"
    restart: unless-stopped
    depends_on: [reverse-proxy, redis]
    env_file:
      - .env
    environment:
      REDIS_HOST: redis
      REDIS_PORT: 6379
    networks:
      - flybeth-net
    labels:
      - "traefik.enable=true"
      # ─── NO CORS middleware at Traefik level ───
      # CORS is handled entirely by NestJS to avoid duplicate headers.
      # HTTPS router
      - "traefik.http.routers.api.rule=Host(`api.flybeth.com`)"
      - "traefik.http.routers.api.entrypoints=websecure"
      - "traefik.http.routers.api.tls.certresolver=flybethresolver"
      - "traefik.http.services.api.loadbalancer.server.port=3000"

networks:
  flybeth-net:

volumes:
  redis_data:
